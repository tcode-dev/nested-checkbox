/**
 * Search
 */
class Search {
    /**
     * count
     * mockなのでランダムな数値を返す
     * reutnr {number}
     */
    count() {
        return Math.round(Math.random() * 1000);
    }
}

module.exports = Search;
